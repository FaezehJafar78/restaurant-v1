import React, { fragment } from 'react';
import stylesheet from './Banner.module.css';

const Banner = () => {
return (
  <fragment>
    <section className={stylesheet.banner}>jk</section>
  </fragment>
);
};
export default Banner;
